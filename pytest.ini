[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    mlflow2: Tests specific to MLflow 2.x
    mlflow3: Tests specific to MLflow 3.x
    slow: Slow running tests
filterwarnings =
    ignore::DeprecationWarning
